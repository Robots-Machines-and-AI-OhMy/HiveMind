"""
things to make:
    Cluster Manager; interpret raft data
    Policy Manager; determine where packets CANNOT go or who CANNOT join
    Translator; turns intercepted processes to container spec [container preparation system]
    Process filter; determine which processes SHOULDN'T be offloaded
"""